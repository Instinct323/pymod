# Installation

Select preferences and run the command to install [PyTorch >= 2.5.1](https://pytorch.org/get-started/previous-versions/) locally.

Install dependencies:

```bash
pip install -r requirements.txt
```
